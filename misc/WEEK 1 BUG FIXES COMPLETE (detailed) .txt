
**BUG-PATCH-1 — Booking temporal guards + AM/PM label**
- Backend rejects past dates and already-passed time slots (Asia/Manila timezone)
- SlotPicker filters past slots client-side before display
- AM/PM label now derived from actual start_time value, not hardcoded field name

**BUG-PATCH-2 — Registration input validation**
- Frontend: per-field inline validation on blur and on Next press
- Name fields reject digits, mobile enforces 09XXXXXXXXX, birthday guards future/pre-1900/under-15
- Backend: mirrors all rules as a second layer via format checks in patient_routes.py

**BUG-PATCH-3 — SC/PWD guard + pending reschedule guard + UX polish**
- SC/PWD discount disabled and explained when no ID is on patient profile
- Backend rejects SC/PWD discount if profile has no sc_pwd_id_number
- Reschedule action hidden on pending appointments; replaced with plain-language guidance
- Status badges capitalized across Dashboard, Appointments, Drawer
- API errors mapped to plain-language messages inline
- Navbar initials update reactively after profile name change

**FB-PAYMENT-1 — Payment type rework**
- SC/PWD separated from payment_type into new discount_type column (migration run)
- HMO sub-selector shows clinician's accredited HMOs when HMO selected
- payment_type stored as "Private" or "HMO:\<name\>", displayed as "HMO — \<name\>"
- Discount row shown separately in AppointmentDrawer and ClinicianDashboard

**F-TRIAGE-PATCH-1 — Triage collapsed to 2 steps**
- First-visit yes/no step removed from GuidedSearch.jsx and KioskTriageScreen.jsx
- Flow is now HMO → Symptom → Results, step indicator reads "Step X of 2"

**F-PATCH-UX-2 — Specialty combobox in clinician directory**
- Specialization filter in /doctors now shows live suggestions via HTML datalist
- Options derived from fetched clinician data, sorted alphabetically

**BUG-PATCH-4 — Birthday error reword + MobX investigation**
- All birthday validation errors collapsed to "Invalid date."
- MobX console error confirmed external (browser DevTools extension) — not application code, ruled out as non-issue

**BUG-PATCH-5 — triageLogic.js sync + standing rule**
- Files were already identical; sync warning comment added to both
- Standing rule established: any triageLogic.js change must be applied to both copies

---

### Carried forward (not addressed this branch)

- **UX-3** — Triage discoverability for new users (entry point not obvious before login)
- **UX-5** — AppointmentDrawer doesn't fill viewport bottom on some browsers
- **BUG-6** — Ruled out (external)
- **F-I18N-1** — Multi-language support (EN/TL) 
- All post-SRET items unchanged

