BUG FIX LOG (Covers April 6-11 Reports) : 

BF 1 - 
● All good. Here's a summary of every change made:

  ---
  Backend — appointment_routes.py

  - Imported ZoneInfo from stdlib zoneinfo
  - Added temporal guard in create_appointment() after the clinician FK check, using datetime.now(ZoneInfo("Asia/Manila")):
    - slot_date < today_ph → 400 "Appointment date has already passed."
    - slot_date == today_ph AND start_time <= now_ph.time() → 400 "This time slot has already passed for today."

  Frontend — SlotPicker.jsx (two fixes)

  1. AM/PM label bug — generateSlots() was hardcoding period: 'AM' and period: 'PM' based on which schedule field (am_start/pm_start) was
  present, not the actual time value. Changed both to parseInt(start_time.slice(0, 2), 10) < 12 ? 'AM' : 'PM'. The availableSlots path (line
   137) was already correct.
  2. Past-slot display filter — In the availableSlots branch, when selectedDate === today, slots are now filtered to only those where
  start_time > current local time before rendering. Slots on past dates can't be selected anyway (date input min is today), so only today
  needs the time comparison.

✻ Brewed for 1m 49s

BF 2 - 
● Done. Here's a summary of every change:

  ---
  Frontend — Register.jsx

  - Added parseDateLocal(dateStr) helper (splits YYYY-MM-DD into new Date(y, m-1, d) to avoid UTC offset issues with type="date" values)
  - Replaced validateStep with format-checked version:
    - first_name / last_name: required + /\d/ check (no digits)
    - middle_name: optional — only checks if non-empty
    - birthday: required + local date parsing + future-date guard + pre-1900 guard + 15-year minimum age
    - mobile_number: required + /^09\d{9}$/ after stripping spaces
    - next_of_kin_name (step 3): optional, validates digits only if non-empty
    - next_of_kin_contact (step 3): optional, validates mobile format only if non-empty
  - Added handleBlur(field) — runs validateStep for the current step, applies only the blurred field's error
  - Added onBlur={() => handleBlur('...')} to: first_name, last_name, middle_name, birthday, mobile_number, next_of_kin_name,
  next_of_kin_contact
  - Fixed inputCls(false) → inputCls(!!errors.field) for middle_name, next_of_kin_name, next_of_kin_contact (errors were computed but ring
  color wasn't applied)

  Backend — patient_routes.py

  - Added import re and from datetime import date
  - Added format validation block in create_patient() after require_fields, before the Patient(...) constructor:
    - first_name / last_name: /\d/ check → 422
    - mobile_number: strip spaces, re.fullmatch(r"09\d{9}", ...) → 422
    - birthday: date.fromisoformat() parse → 422 on invalid; future date → 422; pre-1900 → 422; under 15 → 422

✻ Sautéed for 2m 32s

BF 3 -
● All 5 items done. Summary of every change:

  ---
  AuthContext.jsx — Added refreshUser callback: fetches GET /api/patients/<id> and merges first_name/last_name into the user state. Exposed
  in context value. Only runs for patient role.

  UpdateProfile.jsx — Destructures refreshUser from useAuth(), calls refreshUser() immediately after a successful PATCH /api/patients/<id>.
  Navbar initials update reactively because initials is derived directly from user on every render.

  AppointmentDrawer.jsx
  - canReschedule narrowed to appt.status === 'accepted' only (was also 'pending')
  - hasActions extended to include appt.status === 'pending' so the footer still renders
  - Added pending informational paragraph above the Cancel button for pending appointments
  - Added capitalize helper for the fallback badge label

  PatientAppointments.jsx
  - Added capitalize helper (same formula)
  - Status badge now uses capitalize(appt.status) instead of .replace(/_/g, ' ')
  - "Request Reschedule" button now gated by eligible && appt.status !== 'pending'

  PatientDashboard.jsx
  - Added capitalize helper
  - Status badge in recent appointments list now uses capitalize(appt.status)

  BookAppointment.jsx
  - Added patient state + useEffect to fetch GET /api/patients/<user.id> on mount
  - Added hasSCPWD = !!(patient?.sc_pwd_id_number) derived value
  - Added mapApiError(err) helper mapping 400/403/409/422/500 to plain-language strings; handleSubmit uses it
  - SC/PWD <option> elements now have disabled when !hasSCPWD, with "(ID required)" suffix in label
  - Helper text below the Payment Type select when !hasSCPWD, linking to /dashboard/profile

  appointment_routes.py (backend)
  - Stored the get_or_404(Patient, ...) result into patient variable
  - Added SC/PWD guard: if payment_type contains "Senior Citizen" or "PWD" and patient.sc_pwd_id_number is falsy → 403 with plain message

✻ Brewed for 3m 44s