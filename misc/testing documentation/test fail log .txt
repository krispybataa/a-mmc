Run make test-backend
cd a-mmc_backend && python -m pytest -vv --cov=app --cov-fail-under=70 tests/
============================= test session starts ==============================
platform linux -- Python 3.11.15, pytest-9.0.2, pluggy-1.6.0 -- /opt/hostedtoolcache/Python/3.11.15/x64/bin/python
cachedir: .pytest_cache
rootdir: /home/runner/work/a-mmc/a-mmc/a-mmc_backend
plugins: cov-7.1.0
collecting ... collected 32 items

tests/test_appointment_service.py::TestHasOverlapReturnsFalse::test_no_existing_appointments PASSED [  3%]
tests/test_appointment_service.py::TestHasOverlapReturnsFalse::test_touching_boundary_candidate_end_equals_existing_start PASSED [  6%]
tests/test_appointment_service.py::TestHasOverlapReturnsFalse::test_touching_boundary_existing_end_equals_candidate_start PASSED [  9%]
tests/test_appointment_service.py::TestHasOverlapReturnsFalse::test_exclude_appointment_id_removes_self_conflict PASSED [ 12%]
tests/test_appointment_service.py::TestHasOverlapReturnsFalse::test_cancelled_appointment_does_not_trigger_overlap PASSED [ 15%]
tests/test_appointment_service.py::TestHasOverlapReturnsTrue::test_exact_same_slot PASSED [ 18%]
tests/test_appointment_service.py::TestHasOverlapReturnsTrue::test_candidate_starts_inside_existing PASSED [ 21%]
tests/test_appointment_service.py::TestHasOverlapReturnsTrue::test_candidate_ends_inside_existing PASSED [ 25%]
tests/test_appointment_service.py::TestHasOverlapReturnsTrue::test_existing_fully_inside_candidate PASSED [ 28%]
tests/test_appointment_service.py::TestHasOverlapQueryConstruction::test_query_uses_active_status_filter PASSED [ 31%]
tests/test_appointment_service.py::TestHasOverlapQueryConstruction::test_exclusion_adds_second_filter_call PASSED [ 34%]
tests/test_appointment_service.py::TestHasOverlapQueryConstruction::test_no_exclusion_does_not_add_second_filter_call PASSED [ 37%]
tests/test_timeslot_service.py::TestAllSafeOrphans::test_deleted_count_correct FAILED [ 40%]
tests/test_timeslot_service.py::TestAllSafeOrphans::test_stuck_list_empty FAILED [ 43%]
tests/test_timeslot_service.py::TestAllSafeOrphans::test_delete_called_for_safe_orphan FAILED [ 46%]
tests/test_timeslot_service.py::TestAllSafeOrphans::test_commit_called_once FAILED [ 50%]
tests/test_timeslot_service.py::TestAllStuckOrphans::test_deleted_count_zero FAILED [ 53%]
tests/test_timeslot_service.py::TestAllStuckOrphans::test_stuck_list_populated FAILED [ 56%]
tests/test_timeslot_service.py::TestAllStuckOrphans::test_stuck_entry_has_correct_time_strings FAILED [ 59%]
tests/test_timeslot_service.py::TestAllStuckOrphans::test_delete_not_called FAILED [ 62%]
tests/test_timeslot_service.py::TestAllStuckOrphans::test_commit_still_called FAILED [ 65%]
tests/test_timeslot_service.py::TestMixedOrphans::test_deleted_count_is_only_safe FAILED [ 68%]
tests/test_timeslot_service.py::TestMixedOrphans::test_stuck_list_contains_only_stuck FAILED [ 71%]
tests/test_timeslot_service.py::TestMixedOrphans::test_delete_called_only_for_safe FAILED [ 75%]
tests/test_timeslot_service.py::TestNoOrphans::test_returns_zero_deleted FAILED [ 78%]
tests/test_timeslot_service.py::TestNoOrphans::test_delete_not_called FAILED [ 81%]
tests/test_timeslot_service.py::TestNoOrphans::test_commit_called FAILED [ 84%]
tests/test_timeslot_service.py::TestOrphanClassificationEdgeCases::test_available_slot_with_active_appointments_is_stuck FAILED [ 87%]
tests/test_timeslot_service.py::TestOrphanClassificationEdgeCases::test_blocked_slot_with_no_appointments_is_left_in_place FAILED [ 90%]
tests/test_timeslot_service.py::TestTransactionalBehavior::test_exception_during_delete_triggers_rollback FAILED [ 93%]
tests/test_timeslot_service.py::TestTransactionalBehavior::test_exception_during_delete_does_not_commit FAILED [ 96%]
tests/test_timeslot_service.py::TestTransactionalBehavior::test_exception_during_commit_triggers_rollback FAILED [100%]
ERROR: Coverage failure: total of 34 is less than fail-under=70


=================================== FAILURES ===================================
________________ TestAllSafeOrphans.test_deleted_count_correct _________________

self = <test_timeslot_service.TestAllSafeOrphans object at 0x7fc3a1f188d0>

    def test_deleted_count_correct(self):
        with _mocked([SLOT_IN_SCHEDULE, SLOT_ORPHAN_SAFE]):
>           result = regenerate_slots_for_schedule_change(
                CLINICIAN_ID, FROM_DATE, TO_DATE, DURATION
            )

tests/test_timeslot_service.py:164: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

clinician_id = 1, from_date = datetime.date(2026, 3, 30)
to_date = datetime.date(2026, 3, 30), slot_duration_minutes = 60

    def regenerate_slots_for_schedule_change(
        clinician_id: int,
        from_date: date,
        to_date: date,
        slot_duration_minutes: int = 60,
    ) -> dict:
        """
        Handle schedule changes by inserting new slots and removing orphaned ones.
    
        Call this after a ClinicianSchedule row is modified (times changed or day
        toggled). Operates over the full [from_date, to_date] range — all slots for
        the clinician in that window are checked against the updated schedule.
    
        Algorithm
        ---------
        1. Call generate_slots() with commit=False — inserts any new slots that match
           the updated schedule into the session (no commit yet).
        2. Derive the expected (slot_date, start_time_str) key set from the current
           ClinicianSchedule rows (post-edit values, visible via session autoflush).
        3. Query all ClinicianTimeslot rows for the clinician in [from_date, to_date].
           Any slot whose key is NOT in the expected set is an orphan.
        4. Classify orphans:
           - Stuck:       has one or more active appointments (pending | accepted |
                          reschedule_requested), regardless of slot status.
                          Left in place; returned in the result for C/S to resolve.
           - Safe orphan: status == "available" AND zero active appointments.
                          Deleted from the DB.
           - Blocked with no active appointments: left in place (C/S explicitly blocked it).
        5. Commit — a single commit covers both the generate_slots inserts and the
           safe orphan deletes (atomic with the schedule update if the caller flushed
           the schedule change before invoking this function).
    
        Transactional contract
        ----------------------
        This function wraps the full operation in a try/except:
          - On success: commits once and returns.
          - On failure: rolls back the session and re-raises.
    
        The schedule-edit route should flush the schedule update BEFORE calling this,
        so that generate_slots() queries the updated schedule values. The route must
        NOT commit before the call; the single commit here covers everything atomically.
    
        Returns
        -------
        {
            "deleted": int,     # count of safe orphans deleted
            "stuck": [          # slots needing manual C/S action before they can be removed
                {
                    "slot_id": int,
                    "slot_date": str,    # ISO date string
                    "start_time": str,   # HH:MM
                    "end_time": str,     # HH:MM
                    "active_appointment_count": int,
                },
                ...
            ]
        }
        """
        _ACTIVE = ("pending", "accepted", "reschedule_requested")
    
        try:
            # Step 1: Insert new slots matching the updated schedule (no commit yet)
            generate_slots(clinician_id, from_date, to_date, slot_duration_minutes, commit=False)
    
            # Step 2: Derive expected keys from the current (post-edit) schedule
            expected_keys = _compute_expected_keys(
                clinician_id, from_date, to_date, slot_duration_minutes
            )
    
            # Step 3: Query all timeslots in range.
            # SQLAlchemy autoflushes before this query, so generate_slots inserts are visible.
            all_slots = ClinicianTimeslot.query.filter(
                ClinicianTimeslot.clinician_id == clinician_id,
>               ClinicianTimeslot.slot_date >= from_date,
                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
                ClinicianTimeslot.slot_date <= to_date,
            ).all()
E           TypeError: '>=' not supported between instances of 'MagicMock' and 'datetime.date'

app/services/timeslot_service.py:268: TypeError
___________________ TestAllSafeOrphans.test_stuck_list_empty ___________________

self = <test_timeslot_service.TestAllSafeOrphans object at 0x7fc3a1f18b50>

    def test_stuck_list_empty(self):
        with _mocked([SLOT_IN_SCHEDULE, SLOT_ORPHAN_SAFE]):
>           result = regenerate_slots_for_schedule_change(
                CLINICIAN_ID, FROM_DATE, TO_DATE, DURATION
            )

tests/test_timeslot_service.py:171: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

clinician_id = 1, from_date = datetime.date(2026, 3, 30)
to_date = datetime.date(2026, 3, 30), slot_duration_minutes = 60

    def regenerate_slots_for_schedule_change(
        clinician_id: int,
        from_date: date,
        to_date: date,
        slot_duration_minutes: int = 60,
    ) -> dict:
        """
        Handle schedule changes by inserting new slots and removing orphaned ones.
    
        Call this after a ClinicianSchedule row is modified (times changed or day
        toggled). Operates over the full [from_date, to_date] range — all slots for
        the clinician in that window are checked against the updated schedule.
    
        Algorithm
        ---------
        1. Call generate_slots() with commit=False — inserts any new slots that match
           the updated schedule into the session (no commit yet).
        2. Derive the expected (slot_date, start_time_str) key set from the current
           ClinicianSchedule rows (post-edit values, visible via session autoflush).
        3. Query all ClinicianTimeslot rows for the clinician in [from_date, to_date].
           Any slot whose key is NOT in the expected set is an orphan.
        4. Classify orphans:
           - Stuck:       has one or more active appointments (pending | accepted |
                          reschedule_requested), regardless of slot status.
                          Left in place; returned in the result for C/S to resolve.
           - Safe orphan: status == "available" AND zero active appointments.
                          Deleted from the DB.
           - Blocked with no active appointments: left in place (C/S explicitly blocked it).
        5. Commit — a single commit covers both the generate_slots inserts and the
           safe orphan deletes (atomic with the schedule update if the caller flushed
           the schedule change before invoking this function).
    
        Transactional contract
        ----------------------
        This function wraps the full operation in a try/except:
          - On success: commits once and returns.
          - On failure: rolls back the session and re-raises.
    
        The schedule-edit route should flush the schedule update BEFORE calling this,
        so that generate_slots() queries the updated schedule values. The route must
        NOT commit before the call; the single commit here covers everything atomically.
    
        Returns
        -------
        {
            "deleted": int,     # count of safe orphans deleted
            "stuck": [          # slots needing manual C/S action before they can be removed
                {
                    "slot_id": int,
                    "slot_date": str,    # ISO date string
                    "start_time": str,   # HH:MM
                    "end_time": str,     # HH:MM
                    "active_appointment_count": int,
                },
                ...
            ]
        }
        """
        _ACTIVE = ("pending", "accepted", "reschedule_requested")
    
        try:
            # Step 1: Insert new slots matching the updated schedule (no commit yet)
            generate_slots(clinician_id, from_date, to_date, slot_duration_minutes, commit=False)
    
            # Step 2: Derive expected keys from the current (post-edit) schedule
            expected_keys = _compute_expected_keys(
                clinician_id, from_date, to_date, slot_duration_minutes
            )
    
            # Step 3: Query all timeslots in range.
            # SQLAlchemy autoflushes before this query, so generate_slots inserts are visible.
            all_slots = ClinicianTimeslot.query.filter(
                ClinicianTimeslot.clinician_id == clinician_id,
>               ClinicianTimeslot.slot_date >= from_date,
                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
                ClinicianTimeslot.slot_date <= to_date,
            ).all()
E           TypeError: '>=' not supported between instances of 'MagicMock' and 'datetime.date'

app/services/timeslot_service.py:268: TypeError
____________ TestAllSafeOrphans.test_delete_called_for_safe_orphan _____________

self = <test_timeslot_service.TestAllSafeOrphans object at 0x7fc3a1f18050>

    def test_delete_called_for_safe_orphan(self):
        with _mocked([SLOT_IN_SCHEDULE, SLOT_ORPHAN_SAFE]) as mock_db:
>           regenerate_slots_for_schedule_change(
                CLINICIAN_ID, FROM_DATE, TO_DATE, DURATION
            )

tests/test_timeslot_service.py:178: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

clinician_id = 1, from_date = datetime.date(2026, 3, 30)
to_date = datetime.date(2026, 3, 30), slot_duration_minutes = 60

    def regenerate_slots_for_schedule_change(
        clinician_id: int,
        from_date: date,
        to_date: date,
        slot_duration_minutes: int = 60,
    ) -> dict:
        """
        Handle schedule changes by inserting new slots and removing orphaned ones.
    
        Call this after a ClinicianSchedule row is modified (times changed or day
        toggled). Operates over the full [from_date, to_date] range — all slots for
        the clinician in that window are checked against the updated schedule.
    
        Algorithm
        ---------
        1. Call generate_slots() with commit=False — inserts any new slots that match
           the updated schedule into the session (no commit yet).
        2. Derive the expected (slot_date, start_time_str) key set from the current
           ClinicianSchedule rows (post-edit values, visible via session autoflush).
        3. Query all ClinicianTimeslot rows for the clinician in [from_date, to_date].
           Any slot whose key is NOT in the expected set is an orphan.
        4. Classify orphans:
           - Stuck:       has one or more active appointments (pending | accepted |
                          reschedule_requested), regardless of slot status.
                          Left in place; returned in the result for C/S to resolve.
           - Safe orphan: status == "available" AND zero active appointments.
                          Deleted from the DB.
           - Blocked with no active appointments: left in place (C/S explicitly blocked it).
        5. Commit — a single commit covers both the generate_slots inserts and the
           safe orphan deletes (atomic with the schedule update if the caller flushed
           the schedule change before invoking this function).
    
        Transactional contract
        ----------------------
        This function wraps the full operation in a try/except:
          - On success: commits once and returns.
          - On failure: rolls back the session and re-raises.
    
        The schedule-edit route should flush the schedule update BEFORE calling this,
        so that generate_slots() queries the updated schedule values. The route must
        NOT commit before the call; the single commit here covers everything atomically.
    
        Returns
        -------
        {
            "deleted": int,     # count of safe orphans deleted
            "stuck": [          # slots needing manual C/S action before they can be removed
                {
                    "slot_id": int,
                    "slot_date": str,    # ISO date string
                    "start_time": str,   # HH:MM
                    "end_time": str,     # HH:MM
                    "active_appointment_count": int,
                },
                ...
            ]
        }
        """
        _ACTIVE = ("pending", "accepted", "reschedule_requested")
    
        try:
            # Step 1: Insert new slots matching the updated schedule (no commit yet)
            generate_slots(clinician_id, from_date, to_date, slot_duration_minutes, commit=False)
    
            # Step 2: Derive expected keys from the current (post-edit) schedule
            expected_keys = _compute_expected_keys(
                clinician_id, from_date, to_date, slot_duration_minutes
            )
    
            # Step 3: Query all timeslots in range.
            # SQLAlchemy autoflushes before this query, so generate_slots inserts are visible.
            all_slots = ClinicianTimeslot.query.filter(
                ClinicianTimeslot.clinician_id == clinician_id,
>               ClinicianTimeslot.slot_date >= from_date,
                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
                ClinicianTimeslot.slot_date <= to_date,
            ).all()
E           TypeError: '>=' not supported between instances of 'MagicMock' and 'datetime.date'

app/services/timeslot_service.py:268: TypeError
__________________ TestAllSafeOrphans.test_commit_called_once __________________

self = <test_timeslot_service.TestAllSafeOrphans object at 0x7fc3a1f19310>

    def test_commit_called_once(self):
        with _mocked([SLOT_IN_SCHEDULE, SLOT_ORPHAN_SAFE]) as mock_db:
>           regenerate_slots_for_schedule_change(
                CLINICIAN_ID, FROM_DATE, TO_DATE, DURATION
            )

tests/test_timeslot_service.py:185: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

clinician_id = 1, from_date = datetime.date(2026, 3, 30)
to_date = datetime.date(2026, 3, 30), slot_duration_minutes = 60

    def regenerate_slots_for_schedule_change(
        clinician_id: int,
        from_date: date,
        to_date: date,
        slot_duration_minutes: int = 60,
    ) -> dict:
        """
        Handle schedule changes by inserting new slots and removing orphaned ones.
    
        Call this after a ClinicianSchedule row is modified (times changed or day
        toggled). Operates over the full [from_date, to_date] range — all slots for
        the clinician in that window are checked against the updated schedule.
    
        Algorithm
        ---------
        1. Call generate_slots() with commit=False — inserts any new slots that match
           the updated schedule into the session (no commit yet).
        2. Derive the expected (slot_date, start_time_str) key set from the current
           ClinicianSchedule rows (post-edit values, visible via session autoflush).
        3. Query all ClinicianTimeslot rows for the clinician in [from_date, to_date].
           Any slot whose key is NOT in the expected set is an orphan.
        4. Classify orphans:
           - Stuck:       has one or more active appointments (pending | accepted |
                          reschedule_requested), regardless of slot status.
                          Left in place; returned in the result for C/S to resolve.
           - Safe orphan: status == "available" AND zero active appointments.
                          Deleted from the DB.
           - Blocked with no active appointments: left in place (C/S explicitly blocked it).
        5. Commit — a single commit covers both the generate_slots inserts and the
           safe orphan deletes (atomic with the schedule update if the caller flushed
           the schedule change before invoking this function).
    
        Transactional contract
        ----------------------
        This function wraps the full operation in a try/except:
          - On success: commits once and returns.
          - On failure: rolls back the session and re-raises.
    
        The schedule-edit route should flush the schedule update BEFORE calling this,
        so that generate_slots() queries the updated schedule values. The route must
        NOT commit before the call; the single commit here covers everything atomically.
    
        Returns
        -------
        {
            "deleted": int,     # count of safe orphans deleted
            "stuck": [          # slots needing manual C/S action before they can be removed
                {
                    "slot_id": int,
                    "slot_date": str,    # ISO date string
                    "start_time": str,   # HH:MM
                    "end_time": str,     # HH:MM
                    "active_appointment_count": int,
                },
                ...
            ]
        }
        """
        _ACTIVE = ("pending", "accepted", "reschedule_requested")
    
        try:
            # Step 1: Insert new slots matching the updated schedule (no commit yet)
            generate_slots(clinician_id, from_date, to_date, slot_duration_minutes, commit=False)
    
            # Step 2: Derive expected keys from the current (post-edit) schedule
            expected_keys = _compute_expected_keys(
                clinician_id, from_date, to_date, slot_duration_minutes
            )
    
            # Step 3: Query all timeslots in range.
            # SQLAlchemy autoflushes before this query, so generate_slots inserts are visible.
            all_slots = ClinicianTimeslot.query.filter(
                ClinicianTimeslot.clinician_id == clinician_id,
>               ClinicianTimeslot.slot_date >= from_date,
                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
                ClinicianTimeslot.slot_date <= to_date,
            ).all()
E           TypeError: '>=' not supported between instances of 'MagicMock' and 'datetime.date'

app/services/timeslot_service.py:268: TypeError
_________________ TestAllStuckOrphans.test_deleted_count_zero __________________

self = <test_timeslot_service.TestAllStuckOrphans object at 0x7fc3a1f1a610>

    def test_deleted_count_zero(self):
        with _mocked([SLOT_IN_SCHEDULE, SLOT_ORPHAN_STUCK], {3: 2}):
>           result = regenerate_slots_for_schedule_change(
                CLINICIAN_ID, FROM_DATE, TO_DATE, DURATION
            )

tests/test_timeslot_service.py:196: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

clinician_id = 1, from_date = datetime.date(2026, 3, 30)
to_date = datetime.date(2026, 3, 30), slot_duration_minutes = 60

    def regenerate_slots_for_schedule_change(
        clinician_id: int,
        from_date: date,
        to_date: date,
        slot_duration_minutes: int = 60,
    ) -> dict:
        """
        Handle schedule changes by inserting new slots and removing orphaned ones.
    
        Call this after a ClinicianSchedule row is modified (times changed or day
        toggled). Operates over the full [from_date, to_date] range — all slots for
        the clinician in that window are checked against the updated schedule.
    
        Algorithm
        ---------
        1. Call generate_slots() with commit=False — inserts any new slots that match
           the updated schedule into the session (no commit yet).
        2. Derive the expected (slot_date, start_time_str) key set from the current
           ClinicianSchedule rows (post-edit values, visible via session autoflush).
        3. Query all ClinicianTimeslot rows for the clinician in [from_date, to_date].
           Any slot whose key is NOT in the expected set is an orphan.
        4. Classify orphans:
           - Stuck:       has one or more active appointments (pending | accepted |
                          reschedule_requested), regardless of slot status.
                          Left in place; returned in the result for C/S to resolve.
           - Safe orphan: status == "available" AND zero active appointments.
                          Deleted from the DB.
           - Blocked with no active appointments: left in place (C/S explicitly blocked it).
        5. Commit — a single commit covers both the generate_slots inserts and the
           safe orphan deletes (atomic with the schedule update if the caller flushed
           the schedule change before invoking this function).
    
        Transactional contract
        ----------------------
        This function wraps the full operation in a try/except:
          - On success: commits once and returns.
          - On failure: rolls back the session and re-raises.
    
        The schedule-edit route should flush the schedule update BEFORE calling this,
        so that generate_slots() queries the updated schedule values. The route must
        NOT commit before the call; the single commit here covers everything atomically.
    
        Returns
        -------
        {
            "deleted": int,     # count of safe orphans deleted
            "stuck": [          # slots needing manual C/S action before they can be removed
                {
                    "slot_id": int,
                    "slot_date": str,    # ISO date string
                    "start_time": str,   # HH:MM
                    "end_time": str,     # HH:MM
                    "active_appointment_count": int,
                },
                ...
            ]
        }
        """
        _ACTIVE = ("pending", "accepted", "reschedule_requested")
    
        try:
            # Step 1: Insert new slots matching the updated schedule (no commit yet)
            generate_slots(clinician_id, from_date, to_date, slot_duration_minutes, commit=False)
    
            # Step 2: Derive expected keys from the current (post-edit) schedule
            expected_keys = _compute_expected_keys(
                clinician_id, from_date, to_date, slot_duration_minutes
            )
    
            # Step 3: Query all timeslots in range.
            # SQLAlchemy autoflushes before this query, so generate_slots inserts are visible.
            all_slots = ClinicianTimeslot.query.filter(
                ClinicianTimeslot.clinician_id == clinician_id,
>               ClinicianTimeslot.slot_date >= from_date,
                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
                ClinicianTimeslot.slot_date <= to_date,
            ).all()
E           TypeError: '>=' not supported between instances of 'MagicMock' and 'datetime.date'

app/services/timeslot_service.py:268: TypeError
________________ TestAllStuckOrphans.test_stuck_list_populated _________________

self = <test_timeslot_service.TestAllStuckOrphans object at 0x7fc3a1f19bd0>

    def test_stuck_list_populated(self):
        with _mocked([SLOT_IN_SCHEDULE, SLOT_ORPHAN_STUCK], {3: 2}):
>           result = regenerate_slots_for_schedule_change(
                CLINICIAN_ID, FROM_DATE, TO_DATE, DURATION
            )

tests/test_timeslot_service.py:203: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

clinician_id = 1, from_date = datetime.date(2026, 3, 30)
to_date = datetime.date(2026, 3, 30), slot_duration_minutes = 60

    def regenerate_slots_for_schedule_change(
        clinician_id: int,
        from_date: date,
        to_date: date,
        slot_duration_minutes: int = 60,
    ) -> dict:
        """
        Handle schedule changes by inserting new slots and removing orphaned ones.
    
        Call this after a ClinicianSchedule row is modified (times changed or day
        toggled). Operates over the full [from_date, to_date] range — all slots for
        the clinician in that window are checked against the updated schedule.
    
        Algorithm
        ---------
        1. Call generate_slots() with commit=False — inserts any new slots that match
           the updated schedule into the session (no commit yet).
        2. Derive the expected (slot_date, start_time_str) key set from the current
           ClinicianSchedule rows (post-edit values, visible via session autoflush).
        3. Query all ClinicianTimeslot rows for the clinician in [from_date, to_date].
           Any slot whose key is NOT in the expected set is an orphan.
        4. Classify orphans:
           - Stuck:       has one or more active appointments (pending | accepted |
                          reschedule_requested), regardless of slot status.
                          Left in place; returned in the result for C/S to resolve.
           - Safe orphan: status == "available" AND zero active appointments.
                          Deleted from the DB.
           - Blocked with no active appointments: left in place (C/S explicitly blocked it).
        5. Commit — a single commit covers both the generate_slots inserts and the
           safe orphan deletes (atomic with the schedule update if the caller flushed
           the schedule change before invoking this function).
    
        Transactional contract
        ----------------------
        This function wraps the full operation in a try/except:
          - On success: commits once and returns.
          - On failure: rolls back the session and re-raises.
    
        The schedule-edit route should flush the schedule update BEFORE calling this,
        so that generate_slots() queries the updated schedule values. The route must
        NOT commit before the call; the single commit here covers everything atomically.
    
        Returns
        -------
        {
            "deleted": int,     # count of safe orphans deleted
            "stuck": [          # slots needing manual C/S action before they can be removed
                {
                    "slot_id": int,
                    "slot_date": str,    # ISO date string
                    "start_time": str,   # HH:MM
                    "end_time": str,     # HH:MM
                    "active_appointment_count": int,
                },
                ...
            ]
        }
        """
        _ACTIVE = ("pending", "accepted", "reschedule_requested")
    
        try:
            # Step 1: Insert new slots matching the updated schedule (no commit yet)
            generate_slots(clinician_id, from_date, to_date, slot_duration_minutes, commit=False)
    
            # Step 2: Derive expected keys from the current (post-edit) schedule
            expected_keys = _compute_expected_keys(
                clinician_id, from_date, to_date, slot_duration_minutes
            )
    
            # Step 3: Query all timeslots in range.
            # SQLAlchemy autoflushes before this query, so generate_slots inserts are visible.
            all_slots = ClinicianTimeslot.query.filter(
                ClinicianTimeslot.clinician_id == clinician_id,
>               ClinicianTimeslot.slot_date >= from_date,
                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
                ClinicianTimeslot.slot_date <= to_date,
            ).all()
E           TypeError: '>=' not supported between instances of 'MagicMock' and 'datetime.date'

app/services/timeslot_service.py:268: TypeError
________ TestAllStuckOrphans.test_stuck_entry_has_correct_time_strings _________

self = <test_timeslot_service.TestAllStuckOrphans object at 0x7fc3a1f1a990>

    def test_stuck_entry_has_correct_time_strings(self):
        with _mocked([SLOT_IN_SCHEDULE, SLOT_ORPHAN_STUCK], {3: 1}):
>           result = regenerate_slots_for_schedule_change(
                CLINICIAN_ID, FROM_DATE, TO_DATE, DURATION
            )

tests/test_timeslot_service.py:213: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

clinician_id = 1, from_date = datetime.date(2026, 3, 30)
to_date = datetime.date(2026, 3, 30), slot_duration_minutes = 60

    def regenerate_slots_for_schedule_change(
        clinician_id: int,
        from_date: date,
        to_date: date,
        slot_duration_minutes: int = 60,
    ) -> dict:
        """
        Handle schedule changes by inserting new slots and removing orphaned ones.
    
        Call this after a ClinicianSchedule row is modified (times changed or day
        toggled). Operates over the full [from_date, to_date] range — all slots for
        the clinician in that window are checked against the updated schedule.
    
        Algorithm
        ---------
        1. Call generate_slots() with commit=False — inserts any new slots that match
           the updated schedule into the session (no commit yet).
        2. Derive the expected (slot_date, start_time_str) key set from the current
           ClinicianSchedule rows (post-edit values, visible via session autoflush).
        3. Query all ClinicianTimeslot rows for the clinician in [from_date, to_date].
           Any slot whose key is NOT in the expected set is an orphan.
        4. Classify orphans:
           - Stuck:       has one or more active appointments (pending | accepted |
                          reschedule_requested), regardless of slot status.
                          Left in place; returned in the result for C/S to resolve.
           - Safe orphan: status == "available" AND zero active appointments.
                          Deleted from the DB.
           - Blocked with no active appointments: left in place (C/S explicitly blocked it).
        5. Commit — a single commit covers both the generate_slots inserts and the
           safe orphan deletes (atomic with the schedule update if the caller flushed
           the schedule change before invoking this function).
    
        Transactional contract
        ----------------------
        This function wraps the full operation in a try/except:
          - On success: commits once and returns.
          - On failure: rolls back the session and re-raises.
    
        The schedule-edit route should flush the schedule update BEFORE calling this,
        so that generate_slots() queries the updated schedule values. The route must
        NOT commit before the call; the single commit here covers everything atomically.
    
        Returns
        -------
        {
            "deleted": int,     # count of safe orphans deleted
            "stuck": [          # slots needing manual C/S action before they can be removed
                {
                    "slot_id": int,
                    "slot_date": str,    # ISO date string
                    "start_time": str,   # HH:MM
                    "end_time": str,     # HH:MM
                    "active_appointment_count": int,
                },
                ...
            ]
        }
        """
        _ACTIVE = ("pending", "accepted", "reschedule_requested")
    
        try:
            # Step 1: Insert new slots matching the updated schedule (no commit yet)
            generate_slots(clinician_id, from_date, to_date, slot_duration_minutes, commit=False)
    
            # Step 2: Derive expected keys from the current (post-edit) schedule
            expected_keys = _compute_expected_keys(
                clinician_id, from_date, to_date, slot_duration_minutes
            )
    
            # Step 3: Query all timeslots in range.
            # SQLAlchemy autoflushes before this query, so generate_slots inserts are visible.
            all_slots = ClinicianTimeslot.query.filter(
                ClinicianTimeslot.clinician_id == clinician_id,
>               ClinicianTimeslot.slot_date >= from_date,
                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
                ClinicianTimeslot.slot_date <= to_date,
            ).all()
E           TypeError: '>=' not supported between instances of 'MagicMock' and 'datetime.date'

app/services/timeslot_service.py:268: TypeError
__________________ TestAllStuckOrphans.test_delete_not_called __________________

self = <test_timeslot_service.TestAllStuckOrphans object at 0x7fc3a1f1b710>

    def test_delete_not_called(self):
        with _mocked([SLOT_IN_SCHEDULE, SLOT_ORPHAN_STUCK], {3: 1}) as mock_db:
>           regenerate_slots_for_schedule_change(
                CLINICIAN_ID, FROM_DATE, TO_DATE, DURATION
            )

tests/test_timeslot_service.py:223: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

clinician_id = 1, from_date = datetime.date(2026, 3, 30)
to_date = datetime.date(2026, 3, 30), slot_duration_minutes = 60

    def regenerate_slots_for_schedule_change(
        clinician_id: int,
        from_date: date,
        to_date: date,
        slot_duration_minutes: int = 60,
    ) -> dict:
        """
        Handle schedule changes by inserting new slots and removing orphaned ones.
    
        Call this after a ClinicianSchedule row is modified (times changed or day
        toggled). Operates over the full [from_date, to_date] range — all slots for
        the clinician in that window are checked against the updated schedule.
    
        Algorithm
        ---------
        1. Call generate_slots() with commit=False — inserts any new slots that match
           the updated schedule into the session (no commit yet).
        2. Derive the expected (slot_date, start_time_str) key set from the current
           ClinicianSchedule rows (post-edit values, visible via session autoflush).
        3. Query all ClinicianTimeslot rows for the clinician in [from_date, to_date].
           Any slot whose key is NOT in the expected set is an orphan.
        4. Classify orphans:
           - Stuck:       has one or more active appointments (pending | accepted |
                          reschedule_requested), regardless of slot status.
                          Left in place; returned in the result for C/S to resolve.
           - Safe orphan: status == "available" AND zero active appointments.
                          Deleted from the DB.
           - Blocked with no active appointments: left in place (C/S explicitly blocked it).
        5. Commit — a single commit covers both the generate_slots inserts and the
           safe orphan deletes (atomic with the schedule update if the caller flushed
           the schedule change before invoking this function).
    
        Transactional contract
        ----------------------
        This function wraps the full operation in a try/except:
          - On success: commits once and returns.
          - On failure: rolls back the session and re-raises.
    
        The schedule-edit route should flush the schedule update BEFORE calling this,
        so that generate_slots() queries the updated schedule values. The route must
        NOT commit before the call; the single commit here covers everything atomically.
    
        Returns
        -------
        {
            "deleted": int,     # count of safe orphans deleted
            "stuck": [          # slots needing manual C/S action before they can be removed
                {
                    "slot_id": int,
                    "slot_date": str,    # ISO date string
                    "start_time": str,   # HH:MM
                    "end_time": str,     # HH:MM
                    "active_appointment_count": int,
                },
                ...
            ]
        }
        """
        _ACTIVE = ("pending", "accepted", "reschedule_requested")
    
        try:
            # Step 1: Insert new slots matching the updated schedule (no commit yet)
            generate_slots(clinician_id, from_date, to_date, slot_duration_minutes, commit=False)
    
            # Step 2: Derive expected keys from the current (post-edit) schedule
            expected_keys = _compute_expected_keys(
                clinician_id, from_date, to_date, slot_duration_minutes
            )
    
            # Step 3: Query all timeslots in range.
            # SQLAlchemy autoflushes before this query, so generate_slots inserts are visible.
            all_slots = ClinicianTimeslot.query.filter(
                ClinicianTimeslot.clinician_id == clinician_id,
>               ClinicianTimeslot.slot_date >= from_date,
                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
                ClinicianTimeslot.slot_date <= to_date,
            ).all()
E           TypeError: '>=' not supported between instances of 'MagicMock' and 'datetime.date'

app/services/timeslot_service.py:268: TypeError
_________________ TestAllStuckOrphans.test_commit_still_called _________________

self = <test_timeslot_service.TestAllStuckOrphans object at 0x7fc3a1f1b6d0>

    def test_commit_still_called(self):
        """Commit must run even when there are no deletions."""
        with _mocked([SLOT_IN_SCHEDULE, SLOT_ORPHAN_STUCK], {3: 1}) as mock_db:
>           regenerate_slots_for_schedule_change(
                CLINICIAN_ID, FROM_DATE, TO_DATE, DURATION
            )

tests/test_timeslot_service.py:231: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

clinician_id = 1, from_date = datetime.date(2026, 3, 30)
to_date = datetime.date(2026, 3, 30), slot_duration_minutes = 60

    def regenerate_slots_for_schedule_change(
        clinician_id: int,
        from_date: date,
        to_date: date,
        slot_duration_minutes: int = 60,
    ) -> dict:
        """
        Handle schedule changes by inserting new slots and removing orphaned ones.
    
        Call this after a ClinicianSchedule row is modified (times changed or day
        toggled). Operates over the full [from_date, to_date] range — all slots for
        the clinician in that window are checked against the updated schedule.
    
        Algorithm
        ---------
        1. Call generate_slots() with commit=False — inserts any new slots that match
           the updated schedule into the session (no commit yet).
        2. Derive the expected (slot_date, start_time_str) key set from the current
           ClinicianSchedule rows (post-edit values, visible via session autoflush).
        3. Query all ClinicianTimeslot rows for the clinician in [from_date, to_date].
           Any slot whose key is NOT in the expected set is an orphan.
        4. Classify orphans:
           - Stuck:       has one or more active appointments (pending | accepted |
                          reschedule_requested), regardless of slot status.
                          Left in place; returned in the result for C/S to resolve.
           - Safe orphan: status == "available" AND zero active appointments.
                          Deleted from the DB.
           - Blocked with no active appointments: left in place (C/S explicitly blocked it).
        5. Commit — a single commit covers both the generate_slots inserts and the
           safe orphan deletes (atomic with the schedule update if the caller flushed
           the schedule change before invoking this function).
    
        Transactional contract
        ----------------------
        This function wraps the full operation in a try/except:
          - On success: commits once and returns.
          - On failure: rolls back the session and re-raises.
    
        The schedule-edit route should flush the schedule update BEFORE calling this,
        so that generate_slots() queries the updated schedule values. The route must
        NOT commit before the call; the single commit here covers everything atomically.
    
        Returns
        -------
        {
            "deleted": int,     # count of safe orphans deleted
            "stuck": [          # slots needing manual C/S action before they can be removed
                {
                    "slot_id": int,
                    "slot_date": str,    # ISO date string
                    "start_time": str,   # HH:MM
                    "end_time": str,     # HH:MM
                    "active_appointment_count": int,
                },
                ...
            ]
        }
        """
        _ACTIVE = ("pending", "accepted", "reschedule_requested")
    
        try:
            # Step 1: Insert new slots matching the updated schedule (no commit yet)
            generate_slots(clinician_id, from_date, to_date, slot_duration_minutes, commit=False)
    
            # Step 2: Derive expected keys from the current (post-edit) schedule
            expected_keys = _compute_expected_keys(
                clinician_id, from_date, to_date, slot_duration_minutes
            )
    
            # Step 3: Query all timeslots in range.
            # SQLAlchemy autoflushes before this query, so generate_slots inserts are visible.
            all_slots = ClinicianTimeslot.query.filter(
                ClinicianTimeslot.clinician_id == clinician_id,
>               ClinicianTimeslot.slot_date >= from_date,
                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
                ClinicianTimeslot.slot_date <= to_date,
            ).all()
E           TypeError: '>=' not supported between instances of 'MagicMock' and 'datetime.date'

app/services/timeslot_service.py:268: TypeError
_______________ TestMixedOrphans.test_deleted_count_is_only_safe _______________

self = <test_timeslot_service.TestMixedOrphans object at 0x7fc3a1f25a90>

    def test_deleted_count_is_only_safe(self):
        with _mocked(
            [SLOT_IN_SCHEDULE, SLOT_ORPHAN_SAFE, SLOT_ORPHAN_STUCK],
            {2: 0, 3: 1},
        ):
>           result = regenerate_slots_for_schedule_change(
                CLINICIAN_ID, FROM_DATE, TO_DATE, DURATION
            )

tests/test_timeslot_service.py:245: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

clinician_id = 1, from_date = datetime.date(2026, 3, 30)
to_date = datetime.date(2026, 3, 30), slot_duration_minutes = 60

    def regenerate_slots_for_schedule_change(
        clinician_id: int,
        from_date: date,
        to_date: date,
        slot_duration_minutes: int = 60,
    ) -> dict:
        """
        Handle schedule changes by inserting new slots and removing orphaned ones.
    
        Call this after a ClinicianSchedule row is modified (times changed or day
        toggled). Operates over the full [from_date, to_date] range — all slots for
        the clinician in that window are checked against the updated schedule.
    
        Algorithm
        ---------
        1. Call generate_slots() with commit=False — inserts any new slots that match
           the updated schedule into the session (no commit yet).
        2. Derive the expected (slot_date, start_time_str) key set from the current
           ClinicianSchedule rows (post-edit values, visible via session autoflush).
        3. Query all ClinicianTimeslot rows for the clinician in [from_date, to_date].
           Any slot whose key is NOT in the expected set is an orphan.
        4. Classify orphans:
           - Stuck:       has one or more active appointments (pending | accepted |
                          reschedule_requested), regardless of slot status.
                          Left in place; returned in the result for C/S to resolve.
           - Safe orphan: status == "available" AND zero active appointments.
                          Deleted from the DB.
           - Blocked with no active appointments: left in place (C/S explicitly blocked it).
        5. Commit — a single commit covers both the generate_slots inserts and the
           safe orphan deletes (atomic with the schedule update if the caller flushed
           the schedule change before invoking this function).
    
        Transactional contract
        ----------------------
        This function wraps the full operation in a try/except:
          - On success: commits once and returns.
          - On failure: rolls back the session and re-raises.
    
        The schedule-edit route should flush the schedule update BEFORE calling this,
        so that generate_slots() queries the updated schedule values. The route must
        NOT commit before the call; the single commit here covers everything atomically.
    
        Returns
        -------
        {
            "deleted": int,     # count of safe orphans deleted
            "stuck": [          # slots needing manual C/S action before they can be removed
                {
                    "slot_id": int,
                    "slot_date": str,    # ISO date string
                    "start_time": str,   # HH:MM
                    "end_time": str,     # HH:MM
                    "active_appointment_count": int,
                },
                ...
            ]
        }
        """
        _ACTIVE = ("pending", "accepted", "reschedule_requested")
    
        try:
            # Step 1: Insert new slots matching the updated schedule (no commit yet)
            generate_slots(clinician_id, from_date, to_date, slot_duration_minutes, commit=False)
    
            # Step 2: Derive expected keys from the current (post-edit) schedule
            expected_keys = _compute_expected_keys(
                clinician_id, from_date, to_date, slot_duration_minutes
            )
    
            # Step 3: Query all timeslots in range.
            # SQLAlchemy autoflushes before this query, so generate_slots inserts are visible.
            all_slots = ClinicianTimeslot.query.filter(
                ClinicianTimeslot.clinician_id == clinician_id,
>               ClinicianTimeslot.slot_date >= from_date,
                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
                ClinicianTimeslot.slot_date <= to_date,
            ).all()
E           TypeError: '>=' not supported between instances of 'MagicMock' and 'datetime.date'

app/services/timeslot_service.py:268: TypeError
_____________ TestMixedOrphans.test_stuck_list_contains_only_stuck _____________

self = <test_timeslot_service.TestMixedOrphans object at 0x7fc3a1f26b10>

    def test_stuck_list_contains_only_stuck(self):
        with _mocked(
            [SLOT_IN_SCHEDULE, SLOT_ORPHAN_SAFE, SLOT_ORPHAN_STUCK],
            {2: 0, 3: 1},
        ):
>           result = regenerate_slots_for_schedule_change(
                CLINICIAN_ID, FROM_DATE, TO_DATE, DURATION
            )

tests/test_timeslot_service.py:255: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

clinician_id = 1, from_date = datetime.date(2026, 3, 30)
to_date = datetime.date(2026, 3, 30), slot_duration_minutes = 60

    def regenerate_slots_for_schedule_change(
        clinician_id: int,
        from_date: date,
        to_date: date,
        slot_duration_minutes: int = 60,
    ) -> dict:
        """
        Handle schedule changes by inserting new slots and removing orphaned ones.
    
        Call this after a ClinicianSchedule row is modified (times changed or day
        toggled). Operates over the full [from_date, to_date] range — all slots for
        the clinician in that window are checked against the updated schedule.
    
        Algorithm
        ---------
        1. Call generate_slots() with commit=False — inserts any new slots that match
           the updated schedule into the session (no commit yet).
        2. Derive the expected (slot_date, start_time_str) key set from the current
           ClinicianSchedule rows (post-edit values, visible via session autoflush).
        3. Query all ClinicianTimeslot rows for the clinician in [from_date, to_date].
           Any slot whose key is NOT in the expected set is an orphan.
        4. Classify orphans:
           - Stuck:       has one or more active appointments (pending | accepted |
                          reschedule_requested), regardless of slot status.
                          Left in place; returned in the result for C/S to resolve.
           - Safe orphan: status == "available" AND zero active appointments.
                          Deleted from the DB.
           - Blocked with no active appointments: left in place (C/S explicitly blocked it).
        5. Commit — a single commit covers both the generate_slots inserts and the
           safe orphan deletes (atomic with the schedule update if the caller flushed
           the schedule change before invoking this function).
    
        Transactional contract
        ----------------------
        This function wraps the full operation in a try/except:
          - On success: commits once and returns.
          - On failure: rolls back the session and re-raises.
    
        The schedule-edit route should flush the schedule update BEFORE calling this,
        so that generate_slots() queries the updated schedule values. The route must
        NOT commit before the call; the single commit here covers everything atomically.
    
        Returns
        -------
        {
            "deleted": int,     # count of safe orphans deleted
            "stuck": [          # slots needing manual C/S action before they can be removed
                {
                    "slot_id": int,
                    "slot_date": str,    # ISO date string
                    "start_time": str,   # HH:MM
                    "end_time": str,     # HH:MM
                    "active_appointment_count": int,
                },
                ...
            ]
        }
        """
        _ACTIVE = ("pending", "accepted", "reschedule_requested")
    
        try:
            # Step 1: Insert new slots matching the updated schedule (no commit yet)
            generate_slots(clinician_id, from_date, to_date, slot_duration_minutes, commit=False)
    
            # Step 2: Derive expected keys from the current (post-edit) schedule
            expected_keys = _compute_expected_keys(
                clinician_id, from_date, to_date, slot_duration_minutes
            )
    
            # Step 3: Query all timeslots in range.
            # SQLAlchemy autoflushes before this query, so generate_slots inserts are visible.
            all_slots = ClinicianTimeslot.query.filter(
                ClinicianTimeslot.clinician_id == clinician_id,
>               ClinicianTimeslot.slot_date >= from_date,
                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
                ClinicianTimeslot.slot_date <= to_date,
            ).all()
E           TypeError: '>=' not supported between instances of 'MagicMock' and 'datetime.date'

app/services/timeslot_service.py:268: TypeError
______________ TestMixedOrphans.test_delete_called_only_for_safe _______________

self = <test_timeslot_service.TestMixedOrphans object at 0x7fc3a1f26410>

    def test_delete_called_only_for_safe(self):
        with _mocked(
            [SLOT_IN_SCHEDULE, SLOT_ORPHAN_SAFE, SLOT_ORPHAN_STUCK],
            {2: 0, 3: 1},
        ) as mock_db:
>           regenerate_slots_for_schedule_change(
                CLINICIAN_ID, FROM_DATE, TO_DATE, DURATION
            )

tests/test_timeslot_service.py:266: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

clinician_id = 1, from_date = datetime.date(2026, 3, 30)
to_date = datetime.date(2026, 3, 30), slot_duration_minutes = 60

    def regenerate_slots_for_schedule_change(
        clinician_id: int,
        from_date: date,
        to_date: date,
        slot_duration_minutes: int = 60,
    ) -> dict:
        """
        Handle schedule changes by inserting new slots and removing orphaned ones.
    
        Call this after a ClinicianSchedule row is modified (times changed or day
        toggled). Operates over the full [from_date, to_date] range — all slots for
        the clinician in that window are checked against the updated schedule.
    
        Algorithm
        ---------
        1. Call generate_slots() with commit=False — inserts any new slots that match
           the updated schedule into the session (no commit yet).
        2. Derive the expected (slot_date, start_time_str) key set from the current
           ClinicianSchedule rows (post-edit values, visible via session autoflush).
        3. Query all ClinicianTimeslot rows for the clinician in [from_date, to_date].
           Any slot whose key is NOT in the expected set is an orphan.
        4. Classify orphans:
           - Stuck:       has one or more active appointments (pending | accepted |
                          reschedule_requested), regardless of slot status.
                          Left in place; returned in the result for C/S to resolve.
           - Safe orphan: status == "available" AND zero active appointments.
                          Deleted from the DB.
           - Blocked with no active appointments: left in place (C/S explicitly blocked it).
        5. Commit — a single commit covers both the generate_slots inserts and the
           safe orphan deletes (atomic with the schedule update if the caller flushed
           the schedule change before invoking this function).
    
        Transactional contract
        ----------------------
        This function wraps the full operation in a try/except:
          - On success: commits once and returns.
          - On failure: rolls back the session and re-raises.
    
        The schedule-edit route should flush the schedule update BEFORE calling this,
        so that generate_slots() queries the updated schedule values. The route must
        NOT commit before the call; the single commit here covers everything atomically.
    
        Returns
        -------
        {
            "deleted": int,     # count of safe orphans deleted
            "stuck": [          # slots needing manual C/S action before they can be removed
                {
                    "slot_id": int,
                    "slot_date": str,    # ISO date string
                    "start_time": str,   # HH:MM
                    "end_time": str,     # HH:MM
                    "active_appointment_count": int,
                },
                ...
            ]
        }
        """
        _ACTIVE = ("pending", "accepted", "reschedule_requested")
    
        try:
            # Step 1: Insert new slots matching the updated schedule (no commit yet)
            generate_slots(clinician_id, from_date, to_date, slot_duration_minutes, commit=False)
    
            # Step 2: Derive expected keys from the current (post-edit) schedule
            expected_keys = _compute_expected_keys(
                clinician_id, from_date, to_date, slot_duration_minutes
            )
    
            # Step 3: Query all timeslots in range.
            # SQLAlchemy autoflushes before this query, so generate_slots inserts are visible.
            all_slots = ClinicianTimeslot.query.filter(
                ClinicianTimeslot.clinician_id == clinician_id,
>               ClinicianTimeslot.slot_date >= from_date,
                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
                ClinicianTimeslot.slot_date <= to_date,
            ).all()
E           TypeError: '>=' not supported between instances of 'MagicMock' and 'datetime.date'

app/services/timeslot_service.py:268: TypeError
___________________ TestNoOrphans.test_returns_zero_deleted ____________________

self = <test_timeslot_service.TestNoOrphans object at 0x7fc3a1f26dd0>

    def test_returns_zero_deleted(self):
        with _mocked([SLOT_IN_SCHEDULE]):
>           result = regenerate_slots_for_schedule_change(
                CLINICIAN_ID, FROM_DATE, TO_DATE, DURATION
            )

tests/test_timeslot_service.py:277: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

clinician_id = 1, from_date = datetime.date(2026, 3, 30)
to_date = datetime.date(2026, 3, 30), slot_duration_minutes = 60

    def regenerate_slots_for_schedule_change(
        clinician_id: int,
        from_date: date,
        to_date: date,
        slot_duration_minutes: int = 60,
    ) -> dict:
        """
        Handle schedule changes by inserting new slots and removing orphaned ones.
    
        Call this after a ClinicianSchedule row is modified (times changed or day
        toggled). Operates over the full [from_date, to_date] range — all slots for
        the clinician in that window are checked against the updated schedule.
    
        Algorithm
        ---------
        1. Call generate_slots() with commit=False — inserts any new slots that match
           the updated schedule into the session (no commit yet).
        2. Derive the expected (slot_date, start_time_str) key set from the current
           ClinicianSchedule rows (post-edit values, visible via session autoflush).
        3. Query all ClinicianTimeslot rows for the clinician in [from_date, to_date].
           Any slot whose key is NOT in the expected set is an orphan.
        4. Classify orphans:
           - Stuck:       has one or more active appointments (pending | accepted |
                          reschedule_requested), regardless of slot status.
                          Left in place; returned in the result for C/S to resolve.
           - Safe orphan: status == "available" AND zero active appointments.
                          Deleted from the DB.
           - Blocked with no active appointments: left in place (C/S explicitly blocked it).
        5. Commit — a single commit covers both the generate_slots inserts and the
           safe orphan deletes (atomic with the schedule update if the caller flushed
           the schedule change before invoking this function).
    
        Transactional contract
        ----------------------
        This function wraps the full operation in a try/except:
          - On success: commits once and returns.
          - On failure: rolls back the session and re-raises.
    
        The schedule-edit route should flush the schedule update BEFORE calling this,
        so that generate_slots() queries the updated schedule values. The route must
        NOT commit before the call; the single commit here covers everything atomically.
    
        Returns
        -------
        {
            "deleted": int,     # count of safe orphans deleted
            "stuck": [          # slots needing manual C/S action before they can be removed
                {
                    "slot_id": int,
                    "slot_date": str,    # ISO date string
                    "start_time": str,   # HH:MM
                    "end_time": str,     # HH:MM
                    "active_appointment_count": int,
                },
                ...
            ]
        }
        """
        _ACTIVE = ("pending", "accepted", "reschedule_requested")
    
        try:
            # Step 1: Insert new slots matching the updated schedule (no commit yet)
            generate_slots(clinician_id, from_date, to_date, slot_duration_minutes, commit=False)
    
            # Step 2: Derive expected keys from the current (post-edit) schedule
            expected_keys = _compute_expected_keys(
                clinician_id, from_date, to_date, slot_duration_minutes
            )
    
            # Step 3: Query all timeslots in range.
            # SQLAlchemy autoflushes before this query, so generate_slots inserts are visible.
            all_slots = ClinicianTimeslot.query.filter(
                ClinicianTimeslot.clinician_id == clinician_id,
>               ClinicianTimeslot.slot_date >= from_date,
                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
                ClinicianTimeslot.slot_date <= to_date,
            ).all()
E           TypeError: '>=' not supported between instances of 'MagicMock' and 'datetime.date'

app/services/timeslot_service.py:268: TypeError
_____________________ TestNoOrphans.test_delete_not_called _____________________

self = <test_timeslot_service.TestNoOrphans object at 0x7fc3a1f27290>

    def test_delete_not_called(self):
        with _mocked([SLOT_IN_SCHEDULE]) as mock_db:
>           regenerate_slots_for_schedule_change(
                CLINICIAN_ID, FROM_DATE, TO_DATE, DURATION
            )

tests/test_timeslot_service.py:285: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

clinician_id = 1, from_date = datetime.date(2026, 3, 30)
to_date = datetime.date(2026, 3, 30), slot_duration_minutes = 60

    def regenerate_slots_for_schedule_change(
        clinician_id: int,
        from_date: date,
        to_date: date,
        slot_duration_minutes: int = 60,
    ) -> dict:
        """
        Handle schedule changes by inserting new slots and removing orphaned ones.
    
        Call this after a ClinicianSchedule row is modified (times changed or day
        toggled). Operates over the full [from_date, to_date] range — all slots for
        the clinician in that window are checked against the updated schedule.
    
        Algorithm
        ---------
        1. Call generate_slots() with commit=False — inserts any new slots that match
           the updated schedule into the session (no commit yet).
        2. Derive the expected (slot_date, start_time_str) key set from the current
           ClinicianSchedule rows (post-edit values, visible via session autoflush).
        3. Query all ClinicianTimeslot rows for the clinician in [from_date, to_date].
           Any slot whose key is NOT in the expected set is an orphan.
        4. Classify orphans:
           - Stuck:       has one or more active appointments (pending | accepted |
                          reschedule_requested), regardless of slot status.
                          Left in place; returned in the result for C/S to resolve.
           - Safe orphan: status == "available" AND zero active appointments.
                          Deleted from the DB.
           - Blocked with no active appointments: left in place (C/S explicitly blocked it).
        5. Commit — a single commit covers both the generate_slots inserts and the
           safe orphan deletes (atomic with the schedule update if the caller flushed
           the schedule change before invoking this function).
    
        Transactional contract
        ----------------------
        This function wraps the full operation in a try/except:
          - On success: commits once and returns.
          - On failure: rolls back the session and re-raises.
    
        The schedule-edit route should flush the schedule update BEFORE calling this,
        so that generate_slots() queries the updated schedule values. The route must
        NOT commit before the call; the single commit here covers everything atomically.
    
        Returns
        -------
        {
            "deleted": int,     # count of safe orphans deleted
            "stuck": [          # slots needing manual C/S action before they can be removed
                {
                    "slot_id": int,
                    "slot_date": str,    # ISO date string
                    "start_time": str,   # HH:MM
                    "end_time": str,     # HH:MM
                    "active_appointment_count": int,
                },
                ...
            ]
        }
        """
        _ACTIVE = ("pending", "accepted", "reschedule_requested")
    
        try:
            # Step 1: Insert new slots matching the updated schedule (no commit yet)
            generate_slots(clinician_id, from_date, to_date, slot_duration_minutes, commit=False)
    
            # Step 2: Derive expected keys from the current (post-edit) schedule
            expected_keys = _compute_expected_keys(
                clinician_id, from_date, to_date, slot_duration_minutes
            )
    
            # Step 3: Query all timeslots in range.
            # SQLAlchemy autoflushes before this query, so generate_slots inserts are visible.
            all_slots = ClinicianTimeslot.query.filter(
                ClinicianTimeslot.clinician_id == clinician_id,
>               ClinicianTimeslot.slot_date >= from_date,
                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
                ClinicianTimeslot.slot_date <= to_date,
            ).all()
E           TypeError: '>=' not supported between instances of 'MagicMock' and 'datetime.date'

app/services/timeslot_service.py:268: TypeError
_______________________ TestNoOrphans.test_commit_called _______________________

self = <test_timeslot_service.TestNoOrphans object at 0x7fc3a1f27710>

    def test_commit_called(self):
        with _mocked([SLOT_IN_SCHEDULE]) as mock_db:
>           regenerate_slots_for_schedule_change(
                CLINICIAN_ID, FROM_DATE, TO_DATE, DURATION
            )

tests/test_timeslot_service.py:292: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

clinician_id = 1, from_date = datetime.date(2026, 3, 30)
to_date = datetime.date(2026, 3, 30), slot_duration_minutes = 60

    def regenerate_slots_for_schedule_change(
        clinician_id: int,
        from_date: date,
        to_date: date,
        slot_duration_minutes: int = 60,
    ) -> dict:
        """
        Handle schedule changes by inserting new slots and removing orphaned ones.
    
        Call this after a ClinicianSchedule row is modified (times changed or day
        toggled). Operates over the full [from_date, to_date] range — all slots for
        the clinician in that window are checked against the updated schedule.
    
        Algorithm
        ---------
        1. Call generate_slots() with commit=False — inserts any new slots that match
           the updated schedule into the session (no commit yet).
        2. Derive the expected (slot_date, start_time_str) key set from the current
           ClinicianSchedule rows (post-edit values, visible via session autoflush).
        3. Query all ClinicianTimeslot rows for the clinician in [from_date, to_date].
           Any slot whose key is NOT in the expected set is an orphan.
        4. Classify orphans:
           - Stuck:       has one or more active appointments (pending | accepted |
                          reschedule_requested), regardless of slot status.
                          Left in place; returned in the result for C/S to resolve.
           - Safe orphan: status == "available" AND zero active appointments.
                          Deleted from the DB.
           - Blocked with no active appointments: left in place (C/S explicitly blocked it).
        5. Commit — a single commit covers both the generate_slots inserts and the
           safe orphan deletes (atomic with the schedule update if the caller flushed
           the schedule change before invoking this function).
    
        Transactional contract
        ----------------------
        This function wraps the full operation in a try/except:
          - On success: commits once and returns.
          - On failure: rolls back the session and re-raises.
    
        The schedule-edit route should flush the schedule update BEFORE calling this,
        so that generate_slots() queries the updated schedule values. The route must
        NOT commit before the call; the single commit here covers everything atomically.
    
        Returns
        -------
        {
            "deleted": int,     # count of safe orphans deleted
            "stuck": [          # slots needing manual C/S action before they can be removed
                {
                    "slot_id": int,
                    "slot_date": str,    # ISO date string
                    "start_time": str,   # HH:MM
                    "end_time": str,     # HH:MM
                    "active_appointment_count": int,
                },
                ...
            ]
        }
        """
        _ACTIVE = ("pending", "accepted", "reschedule_requested")
    
        try:
            # Step 1: Insert new slots matching the updated schedule (no commit yet)
            generate_slots(clinician_id, from_date, to_date, slot_duration_minutes, commit=False)
    
            # Step 2: Derive expected keys from the current (post-edit) schedule
            expected_keys = _compute_expected_keys(
                clinician_id, from_date, to_date, slot_duration_minutes
            )
    
            # Step 3: Query all timeslots in range.
            # SQLAlchemy autoflushes before this query, so generate_slots inserts are visible.
            all_slots = ClinicianTimeslot.query.filter(
                ClinicianTimeslot.clinician_id == clinician_id,
>               ClinicianTimeslot.slot_date >= from_date,
                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
                ClinicianTimeslot.slot_date <= to_date,
            ).all()
E           TypeError: '>=' not supported between instances of 'MagicMock' and 'datetime.date'

app/services/timeslot_service.py:268: TypeError
_ TestOrphanClassificationEdgeCases.test_available_slot_with_active_appointments_is_stuck _

self = <test_timeslot_service.TestOrphanClassificationEdgeCases object at 0x7fc3a1f19990>

    def test_available_slot_with_active_appointments_is_stuck(self):
        """
        A slot with status="available" but active appointments is STUCK, not safe.
        Active appointments take precedence over slot status.
        """
        # SLOT_ORPHAN_SAFE has status="available" — but we give it an active appointment
        with _mocked([SLOT_IN_SCHEDULE, SLOT_ORPHAN_SAFE], {2: 1}):
>           result = regenerate_slots_for_schedule_change(
                CLINICIAN_ID, FROM_DATE, TO_DATE, DURATION
            )

tests/test_timeslot_service.py:307: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

clinician_id = 1, from_date = datetime.date(2026, 3, 30)
to_date = datetime.date(2026, 3, 30), slot_duration_minutes = 60

    def regenerate_slots_for_schedule_change(
        clinician_id: int,
        from_date: date,
        to_date: date,
        slot_duration_minutes: int = 60,
    ) -> dict:
        """
        Handle schedule changes by inserting new slots and removing orphaned ones.
    
        Call this after a ClinicianSchedule row is modified (times changed or day
        toggled). Operates over the full [from_date, to_date] range — all slots for
        the clinician in that window are checked against the updated schedule.
    
        Algorithm
        ---------
        1. Call generate_slots() with commit=False — inserts any new slots that match
           the updated schedule into the session (no commit yet).
        2. Derive the expected (slot_date, start_time_str) key set from the current
           ClinicianSchedule rows (post-edit values, visible via session autoflush).
        3. Query all ClinicianTimeslot rows for the clinician in [from_date, to_date].
           Any slot whose key is NOT in the expected set is an orphan.
        4. Classify orphans:
           - Stuck:       has one or more active appointments (pending | accepted |
                          reschedule_requested), regardless of slot status.
                          Left in place; returned in the result for C/S to resolve.
           - Safe orphan: status == "available" AND zero active appointments.
                          Deleted from the DB.
           - Blocked with no active appointments: left in place (C/S explicitly blocked it).
        5. Commit — a single commit covers both the generate_slots inserts and the
           safe orphan deletes (atomic with the schedule update if the caller flushed
           the schedule change before invoking this function).
    
        Transactional contract
        ----------------------
        This function wraps the full operation in a try/except:
          - On success: commits once and returns.
          - On failure: rolls back the session and re-raises.
    
        The schedule-edit route should flush the schedule update BEFORE calling this,
        so that generate_slots() queries the updated schedule values. The route must
        NOT commit before the call; the single commit here covers everything atomically.
    
        Returns
        -------
        {
            "deleted": int,     # count of safe orphans deleted
            "stuck": [          # slots needing manual C/S action before they can be removed
                {
                    "slot_id": int,
                    "slot_date": str,    # ISO date string
                    "start_time": str,   # HH:MM
                    "end_time": str,     # HH:MM
                    "active_appointment_count": int,
                },
                ...
            ]
        }
        """
        _ACTIVE = ("pending", "accepted", "reschedule_requested")
    
        try:
            # Step 1: Insert new slots matching the updated schedule (no commit yet)
            generate_slots(clinician_id, from_date, to_date, slot_duration_minutes, commit=False)
    
            # Step 2: Derive expected keys from the current (post-edit) schedule
            expected_keys = _compute_expected_keys(
                clinician_id, from_date, to_date, slot_duration_minutes
            )
    
            # Step 3: Query all timeslots in range.
            # SQLAlchemy autoflushes before this query, so generate_slots inserts are visible.
            all_slots = ClinicianTimeslot.query.filter(
                ClinicianTimeslot.clinician_id == clinician_id,
>               ClinicianTimeslot.slot_date >= from_date,
                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
                ClinicianTimeslot.slot_date <= to_date,
            ).all()
E           TypeError: '>=' not supported between instances of 'MagicMock' and 'datetime.date'

app/services/timeslot_service.py:268: TypeError
_ TestOrphanClassificationEdgeCases.test_blocked_slot_with_no_appointments_is_left_in_place _

self = <test_timeslot_service.TestOrphanClassificationEdgeCases object at 0x7fc3a1f27850>

    def test_blocked_slot_with_no_appointments_is_left_in_place(self):
        """
        A blocked slot with zero active appointments is neither safe nor stuck —
        it is left in place (C/S explicitly blocked it).
        """
        slot_blocked_orphan = _slot(4, time(10, 0), time(11, 0), status="blocked")
        with _mocked([SLOT_IN_SCHEDULE, slot_blocked_orphan], {4: 0}):
>           result = regenerate_slots_for_schedule_change(
                CLINICIAN_ID, FROM_DATE, TO_DATE, DURATION
            )

tests/test_timeslot_service.py:321: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

clinician_id = 1, from_date = datetime.date(2026, 3, 30)
to_date = datetime.date(2026, 3, 30), slot_duration_minutes = 60

    def regenerate_slots_for_schedule_change(
        clinician_id: int,
        from_date: date,
        to_date: date,
        slot_duration_minutes: int = 60,
    ) -> dict:
        """
        Handle schedule changes by inserting new slots and removing orphaned ones.
    
        Call this after a ClinicianSchedule row is modified (times changed or day
        toggled). Operates over the full [from_date, to_date] range — all slots for
        the clinician in that window are checked against the updated schedule.
    
        Algorithm
        ---------
        1. Call generate_slots() with commit=False — inserts any new slots that match
           the updated schedule into the session (no commit yet).
        2. Derive the expected (slot_date, start_time_str) key set from the current
           ClinicianSchedule rows (post-edit values, visible via session autoflush).
        3. Query all ClinicianTimeslot rows for the clinician in [from_date, to_date].
           Any slot whose key is NOT in the expected set is an orphan.
        4. Classify orphans:
           - Stuck:       has one or more active appointments (pending | accepted |
                          reschedule_requested), regardless of slot status.
                          Left in place; returned in the result for C/S to resolve.
           - Safe orphan: status == "available" AND zero active appointments.
                          Deleted from the DB.
           - Blocked with no active appointments: left in place (C/S explicitly blocked it).
        5. Commit — a single commit covers both the generate_slots inserts and the
           safe orphan deletes (atomic with the schedule update if the caller flushed
           the schedule change before invoking this function).
    
        Transactional contract
        ----------------------
        This function wraps the full operation in a try/except:
          - On success: commits once and returns.
          - On failure: rolls back the session and re-raises.
    
        The schedule-edit route should flush the schedule update BEFORE calling this,
        so that generate_slots() queries the updated schedule values. The route must
        NOT commit before the call; the single commit here covers everything atomically.
    
        Returns
        -------
        {
            "deleted": int,     # count of safe orphans deleted
            "stuck": [          # slots needing manual C/S action before they can be removed
                {
                    "slot_id": int,
                    "slot_date": str,    # ISO date string
                    "start_time": str,   # HH:MM
                    "end_time": str,     # HH:MM
                    "active_appointment_count": int,
                },
                ...
            ]
        }
        """
        _ACTIVE = ("pending", "accepted", "reschedule_requested")
    
        try:
            # Step 1: Insert new slots matching the updated schedule (no commit yet)
            generate_slots(clinician_id, from_date, to_date, slot_duration_minutes, commit=False)
    
            # Step 2: Derive expected keys from the current (post-edit) schedule
            expected_keys = _compute_expected_keys(
                clinician_id, from_date, to_date, slot_duration_minutes
            )
    
            # Step 3: Query all timeslots in range.
            # SQLAlchemy autoflushes before this query, so generate_slots inserts are visible.
            all_slots = ClinicianTimeslot.query.filter(
                ClinicianTimeslot.clinician_id == clinician_id,
>               ClinicianTimeslot.slot_date >= from_date,
                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
                ClinicianTimeslot.slot_date <= to_date,
            ).all()
E           TypeError: '>=' not supported between instances of 'MagicMock' and 'datetime.date'

app/services/timeslot_service.py:268: TypeError
___ TestTransactionalBehavior.test_exception_during_delete_triggers_rollback ___

self = <test_timeslot_service.TestTransactionalBehavior object at 0x7fc3a1f26f90>

    def test_exception_during_delete_triggers_rollback(self):
        """
        If db.session.delete() raises, rollback must be called and the exception
        must propagate to the caller.
        """
        with _mocked([SLOT_IN_SCHEDULE, SLOT_ORPHAN_SAFE]) as mock_db:
            mock_db.session.delete.side_effect = RuntimeError("DB constraint violated")
            with pytest.raises(RuntimeError, match="DB constraint violated"):
>               regenerate_slots_for_schedule_change(
                    CLINICIAN_ID, FROM_DATE, TO_DATE, DURATION
                )

tests/test_timeslot_service.py:338: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

clinician_id = 1, from_date = datetime.date(2026, 3, 30)
to_date = datetime.date(2026, 3, 30), slot_duration_minutes = 60

    def regenerate_slots_for_schedule_change(
        clinician_id: int,
        from_date: date,
        to_date: date,
        slot_duration_minutes: int = 60,
    ) -> dict:
        """
        Handle schedule changes by inserting new slots and removing orphaned ones.
    
        Call this after a ClinicianSchedule row is modified (times changed or day
        toggled). Operates over the full [from_date, to_date] range — all slots for
        the clinician in that window are checked against the updated schedule.
    
        Algorithm
        ---------
        1. Call generate_slots() with commit=False — inserts any new slots that match
           the updated schedule into the session (no commit yet).
        2. Derive the expected (slot_date, start_time_str) key set from the current
           ClinicianSchedule rows (post-edit values, visible via session autoflush).
        3. Query all ClinicianTimeslot rows for the clinician in [from_date, to_date].
           Any slot whose key is NOT in the expected set is an orphan.
        4. Classify orphans:
           - Stuck:       has one or more active appointments (pending | accepted |
                          reschedule_requested), regardless of slot status.
                          Left in place; returned in the result for C/S to resolve.
           - Safe orphan: status == "available" AND zero active appointments.
                          Deleted from the DB.
           - Blocked with no active appointments: left in place (C/S explicitly blocked it).
        5. Commit — a single commit covers both the generate_slots inserts and the
           safe orphan deletes (atomic with the schedule update if the caller flushed
           the schedule change before invoking this function).
    
        Transactional contract
        ----------------------
        This function wraps the full operation in a try/except:
          - On success: commits once and returns.
          - On failure: rolls back the session and re-raises.
    
        The schedule-edit route should flush the schedule update BEFORE calling this,
        so that generate_slots() queries the updated schedule values. The route must
        NOT commit before the call; the single commit here covers everything atomically.
    
        Returns
        -------
        {
            "deleted": int,     # count of safe orphans deleted
            "stuck": [          # slots needing manual C/S action before they can be removed
                {
                    "slot_id": int,
                    "slot_date": str,    # ISO date string
                    "start_time": str,   # HH:MM
                    "end_time": str,     # HH:MM
                    "active_appointment_count": int,
                },
                ...
            ]
        }
        """
        _ACTIVE = ("pending", "accepted", "reschedule_requested")
    
        try:
            # Step 1: Insert new slots matching the updated schedule (no commit yet)
            generate_slots(clinician_id, from_date, to_date, slot_duration_minutes, commit=False)
    
            # Step 2: Derive expected keys from the current (post-edit) schedule
            expected_keys = _compute_expected_keys(
                clinician_id, from_date, to_date, slot_duration_minutes
            )
    
            # Step 3: Query all timeslots in range.
            # SQLAlchemy autoflushes before this query, so generate_slots inserts are visible.
            all_slots = ClinicianTimeslot.query.filter(
                ClinicianTimeslot.clinician_id == clinician_id,
>               ClinicianTimeslot.slot_date >= from_date,
                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
                ClinicianTimeslot.slot_date <= to_date,
            ).all()
E           TypeError: '>=' not supported between instances of 'MagicMock' and 'datetime.date'

app/services/timeslot_service.py:268: TypeError
____ TestTransactionalBehavior.test_exception_during_delete_does_not_commit ____

self = <test_timeslot_service.TestTransactionalBehavior object at 0x7fc3a1f254d0>

    def test_exception_during_delete_does_not_commit(self):
        """If an exception occurs, commit must NOT be called."""
        with _mocked([SLOT_IN_SCHEDULE, SLOT_ORPHAN_SAFE]) as mock_db:
            mock_db.session.delete.side_effect = RuntimeError("failure")
            with pytest.raises(RuntimeError):
>               regenerate_slots_for_schedule_change(
                    CLINICIAN_ID, FROM_DATE, TO_DATE, DURATION
                )

tests/test_timeslot_service.py:348: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

clinician_id = 1, from_date = datetime.date(2026, 3, 30)
to_date = datetime.date(2026, 3, 30), slot_duration_minutes = 60

    def regenerate_slots_for_schedule_change(
        clinician_id: int,
        from_date: date,
        to_date: date,
        slot_duration_minutes: int = 60,
    ) -> dict:
        """
        Handle schedule changes by inserting new slots and removing orphaned ones.
    
        Call this after a ClinicianSchedule row is modified (times changed or day
        toggled). Operates over the full [from_date, to_date] range — all slots for
        the clinician in that window are checked against the updated schedule.
    
        Algorithm
        ---------
        1. Call generate_slots() with commit=False — inserts any new slots that match
           the updated schedule into the session (no commit yet).
        2. Derive the expected (slot_date, start_time_str) key set from the current
           ClinicianSchedule rows (post-edit values, visible via session autoflush).
        3. Query all ClinicianTimeslot rows for the clinician in [from_date, to_date].
           Any slot whose key is NOT in the expected set is an orphan.
        4. Classify orphans:
           - Stuck:       has one or more active appointments (pending | accepted |
                          reschedule_requested), regardless of slot status.
                          Left in place; returned in the result for C/S to resolve.
           - Safe orphan: status == "available" AND zero active appointments.
                          Deleted from the DB.
           - Blocked with no active appointments: left in place (C/S explicitly blocked it).
        5. Commit — a single commit covers both the generate_slots inserts and the
           safe orphan deletes (atomic with the schedule update if the caller flushed
           the schedule change before invoking this function).
    
        Transactional contract
        ----------------------
        This function wraps the full operation in a try/except:
          - On success: commits once and returns.
          - On failure: rolls back the session and re-raises.
    
        The schedule-edit route should flush the schedule update BEFORE calling this,
        so that generate_slots() queries the updated schedule values. The route must
        NOT commit before the call; the single commit here covers everything atomically.
    
        Returns
        -------
        {
            "deleted": int,     # count of safe orphans deleted
            "stuck": [          # slots needing manual C/S action before they can be removed
                {
                    "slot_id": int,
                    "slot_date": str,    # ISO date string
                    "start_time": str,   # HH:MM
                    "end_time": str,     # HH:MM
                    "active_appointment_count": int,
                },
                ...
            ]
        }
        """
        _ACTIVE = ("pending", "accepted", "reschedule_requested")
    
        try:
            # Step 1: Insert new slots matching the updated schedule (no commit yet)
            generate_slots(clinician_id, from_date, to_date, slot_duration_minutes, commit=False)
    
            # Step 2: Derive expected keys from the current (post-edit) schedule
            expected_keys = _compute_expected_keys(
                clinician_id, from_date, to_date, slot_duration_minutes
            )
    
            # Step 3: Query all timeslots in range.
            # SQLAlchemy autoflushes before this query, so generate_slots inserts are visible.
            all_slots = ClinicianTimeslot.query.filter(
                ClinicianTimeslot.clinician_id == clinician_id,
>               ClinicianTimeslot.slot_date >= from_date,
                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
                ClinicianTimeslot.slot_date <= to_date,
            ).all()
E           TypeError: '>=' not supported between instances of 'MagicMock' and 'datetime.date'

app/services/timeslot_service.py:268: TypeError
___ TestTransactionalBehavior.test_exception_during_commit_triggers_rollback ___

self = <test_timeslot_service.TestTransactionalBehavior object at 0x7fc3a1f24b90>

    def test_exception_during_commit_triggers_rollback(self):
        """If db.session.commit() itself raises, rollback must be called."""
        with _mocked([SLOT_IN_SCHEDULE]) as mock_db:
            mock_db.session.commit.side_effect = RuntimeError("commit failed")
            with pytest.raises(RuntimeError, match="commit failed"):
>               regenerate_slots_for_schedule_change(
                    CLINICIAN_ID, FROM_DATE, TO_DATE, DURATION
                )

tests/test_timeslot_service.py:358: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

clinician_id = 1, from_date = datetime.date(2026, 3, 30)
to_date = datetime.date(2026, 3, 30), slot_duration_minutes = 60

    def regenerate_slots_for_schedule_change(
        clinician_id: int,
        from_date: date,
        to_date: date,
        slot_duration_minutes: int = 60,
    ) -> dict:
        """
        Handle schedule changes by inserting new slots and removing orphaned ones.
    
        Call this after a ClinicianSchedule row is modified (times changed or day
        toggled). Operates over the full [from_date, to_date] range — all slots for
        the clinician in that window are checked against the updated schedule.
    
        Algorithm
        ---------
        1. Call generate_slots() with commit=False — inserts any new slots that match
           the updated schedule into the session (no commit yet).
        2. Derive the expected (slot_date, start_time_str) key set from the current
           ClinicianSchedule rows (post-edit values, visible via session autoflush).
        3. Query all ClinicianTimeslot rows for the clinician in [from_date, to_date].
           Any slot whose key is NOT in the expected set is an orphan.
        4. Classify orphans:
           - Stuck:       has one or more active appointments (pending | accepted |
                          reschedule_requested), regardless of slot status.
                          Left in place; returned in the result for C/S to resolve.
           - Safe orphan: status == "available" AND zero active appointments.
                          Deleted from the DB.
           - Blocked with no active appointments: left in place (C/S explicitly blocked it).
        5. Commit — a single commit covers both the generate_slots inserts and the
           safe orphan deletes (atomic with the schedule update if the caller flushed
           the schedule change before invoking this function).
    
        Transactional contract
        ----------------------
        This function wraps the full operation in a try/except:
          - On success: commits once and returns.
          - On failure: rolls back the session and re-raises.
    
        The schedule-edit route should flush the schedule update BEFORE calling this,
        so that generate_slots() queries the updated schedule values. The route must
        NOT commit before the call; the single commit here covers everything atomically.
    
        Returns
        -------
        {
            "deleted": int,     # count of safe orphans deleted
            "stuck": [          # slots needing manual C/S action before they can be removed
                {
                    "slot_id": int,
                    "slot_date": str,    # ISO date string
                    "start_time": str,   # HH:MM
                    "end_time": str,     # HH:MM
                    "active_appointment_count": int,
                },
                ...
            ]
        }
        """
        _ACTIVE = ("pending", "accepted", "reschedule_requested")
    
        try:
            # Step 1: Insert new slots matching the updated schedule (no commit yet)
            generate_slots(clinician_id, from_date, to_date, slot_duration_minutes, commit=False)
    
            # Step 2: Derive expected keys from the current (post-edit) schedule
            expected_keys = _compute_expected_keys(
                clinician_id, from_date, to_date, slot_duration_minutes
            )
    
            # Step 3: Query all timeslots in range.
            # SQLAlchemy autoflushes before this query, so generate_slots inserts are visible.
            all_slots = ClinicianTimeslot.query.filter(
                ClinicianTimeslot.clinician_id == clinician_id,
>               ClinicianTimeslot.slot_date >= from_date,
                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
                ClinicianTimeslot.slot_date <= to_date,
            ).all()
E           TypeError: '>=' not supported between instances of 'MagicMock' and 'datetime.date'

app/services/timeslot_service.py:268: TypeError
================================ tests coverage ================================
_______________ coverage: platform linux, python 3.11.15-final-0 _______________

Name                                  Stmts   Miss  Cover
---------------------------------------------------------
app/__init__.py                          73     49    33%
app/models/__init__.py                    0      0   100%
app/models/admin.py                      10      1    90%
app/models/appointment.py                22      1    95%
app/models/clinician.py                  70      5    93%
app/models/patient.py                    34      1    97%
app/models/secretary.py                  24      2    92%
app/routes/__init__.py                    0      0   100%
app/routes/admin_routes.py              166    119    28%
app/routes/appointment_routes.py        173    147    15%
app/routes/auth_routes.py               136     91    33%
app/routes/clinician_routes.py          170    128    25%
app/routes/patient_routes.py             41     24    41%
app/routes/secretary_routes.py           75     49    35%
app/routes/timeslot_routes.py            53     38    28%
app/seed.py                              84     84     0%
app/services/appointment_service.py      12      0   100%
app/services/auth_service.py             27     15    44%
app/services/email_service.py           103     85    17%
app/services/email_templates.py          71     57    20%
app/services/timeslot_service.py        103     57    45%
app/utils/__init__.py                     0      0   100%
app/utils/validators.py                   6      4    33%
---------------------------------------------------------
TOTAL                                  1453    957    34%
FAIL Required test coverage of 70% not reached. Total coverage: 34.14%
=========================== short test summary info ============================
FAILED tests/test_timeslot_service.py::TestAllSafeOrphans::test_deleted_count_correct - TypeError: '>=' not supported between instances of 'MagicMock' and 'datetime.date'
FAILED tests/test_timeslot_service.py::TestAllSafeOrphans::test_stuck_list_empty - TypeError: '>=' not supported between instances of 'MagicMock' and 'datetime.date'
FAILED tests/test_timeslot_service.py::TestAllSafeOrphans::test_delete_called_for_safe_orphan - TypeError: '>=' not supported between instances of 'MagicMock' and 'datetime.date'
FAILED tests/test_timeslot_service.py::TestAllSafeOrphans::test_commit_called_once - TypeError: '>=' not supported between instances of 'MagicMock' and 'datetime.date'
FAILED tests/test_timeslot_service.py::TestAllStuckOrphans::test_deleted_count_zero - TypeError: '>=' not supported between instances of 'MagicMock' and 'datetime.date'
FAILED tests/test_timeslot_service.py::TestAllStuckOrphans::test_stuck_list_populated - TypeError: '>=' not supported between instances of 'MagicMock' and 'datetime.date'
FAILED tests/test_timeslot_service.py::TestAllStuckOrphans::test_stuck_entry_has_correct_time_strings - TypeError: '>=' not supported between instances of 'MagicMock' and 'datetime.date'
FAILED tests/test_timeslot_service.py::TestAllStuckOrphans::test_delete_not_called - TypeError: '>=' not supported between instances of 'MagicMock' and 'datetime.date'
FAILED tests/test_timeslot_service.py::TestAllStuckOrphans::test_commit_still_called - TypeError: '>=' not supported between instances of 'MagicMock' and 'datetime.date'
FAILED tests/test_timeslot_service.py::TestMixedOrphans::test_deleted_count_is_only_safe - TypeError: '>=' not supported between instances of 'MagicMock' and 'datetime.date'
FAILED tests/test_timeslot_service.py::TestMixedOrphans::test_stuck_list_contains_only_stuck - TypeError: '>=' not supported between instances of 'MagicMock' and 'datetime.date'
FAILED tests/test_timeslot_service.py::TestMixedOrphans::test_delete_called_only_for_safe - TypeError: '>=' not supported between instances of 'MagicMock' and 'datetime.date'
FAILED tests/test_timeslot_service.py::TestNoOrphans::test_returns_zero_deleted - TypeError: '>=' not supported between instances of 'MagicMock' and 'datetime.date'
FAILED tests/test_timeslot_service.py::TestNoOrphans::test_delete_not_called - TypeError: '>=' not supported between instances of 'MagicMock' and 'datetime.date'
FAILED tests/test_timeslot_service.py::TestNoOrphans::test_commit_called - TypeError: '>=' not supported between instances of 'MagicMock' and 'datetime.date'
FAILED tests/test_timeslot_service.py::TestOrphanClassificationEdgeCases::test_available_slot_with_active_appointments_is_stuck - TypeError: '>=' not supported between instances of 'MagicMock' and 'datetime.date'
FAILED tests/test_timeslot_service.py::TestOrphanClassificationEdgeCases::test_blocked_slot_with_no_appointments_is_left_in_place - TypeError: '>=' not supported between instances of 'MagicMock' and 'datetime.date'
FAILED tests/test_timeslot_service.py::TestTransactionalBehavior::test_exception_during_delete_triggers_rollback - TypeError: '>=' not supported between instances of 'MagicMock' and 'datetime.date'
FAILED tests/test_timeslot_service.py::TestTransactionalBehavior::test_exception_during_delete_does_not_commit - TypeError: '>=' not supported between instances of 'MagicMock' and 'datetime.date'
FAILED tests/test_timeslot_service.py::TestTransactionalBehavior::test_exception_during_commit_triggers_rollback - TypeError: '>=' not supported between instances of 'MagicMock' and 'datetime.date'
======================== 20 failed, 12 passed in 1.71s =========================
make: *** [Makefile:2: test-backend] Error 1